apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: patrol-system

resources:
  - namespace.yaml
  - configmap.yaml
  - pvc.yaml
  - deployment.yaml
  - service.yaml
  - ingress.yaml

commonLabels:
  app.kubernetes.io/managed-by: kustomize
  app.kubernetes.io/part-of: patrol-manager-ai
