apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: patrol-manager-data-pvc
  namespace: patrol-system
  labels:
    app.kubernetes.io/name: patrol-manager-ai
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
